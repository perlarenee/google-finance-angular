'use strict';

describe('googleApp.worldmarkets module', function() {

  beforeEach(module('googleApp.worldmarkets'));

  describe('worldmarkets controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var worldMarketsCtrl = $controller('WorldMarketsCtrl');
      expect(worldMarketsCtrl).toBeDefined();
    }));

  });
});